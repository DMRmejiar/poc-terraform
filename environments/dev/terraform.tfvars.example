# =============================================================================
# TERRAFORM VARIABLES EXAMPLE
# =============================================================================
# IMPORTANTE: 
# 1. Copia este archivo como 'terraform.tfvars' 
# 2. Modifica los valores según tu configuración
# 3. NUNCA hagas commit del archivo terraform.tfvars (está en .gitignore)
# =============================================================================

# AWS Configuration
aws_region    = "us-east-1"
instance_type = "t2.micro"
environment   = "dev"

# Optional: Specify a custom AMI ID
# ami_id = "ami-0c02fb55956c7d316"

# Optional: Specify your key pair name for SSH access
# key_name = "your-key-pair-name"

# =============================================================================
# NOTAS DE SEGURIDAD:
# - Nunca incluyas credenciales de AWS en este archivo
# - Usa AWS CLI configurado o variables de entorno para credenciales
# - Usa IAM roles cuando sea posible
# =============================================================================
